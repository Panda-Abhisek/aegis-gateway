info:
  name: Get User by ID
  type: http
  seq: 4
  tags:
    - User Service

http:
  method: GET
  url: "{{baseUrl}}/users/:id"
  params:
    - name: id
      value: "2"
      type: path
  auth:
    type: bearer
    token: "{{JWT}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/users/:id"
      method: GET
      params:
        - name: id
          value: ""
          type: path
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: application/json
      body:
        type: json
        data: |-
          {
            "id": "",
            "name": "",
            "email": "",
            "tier": "",
            "message": ""
          }
