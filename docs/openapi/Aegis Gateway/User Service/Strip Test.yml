info:
  name: Strip Test
  type: http
  seq: 7
  tags:
    - User Service

http:
  method: GET
  url: "{{baseUrl}}/users/strip-test"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/users/strip-test"
      method: GET
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: text/plain
      body:
        type: text
        data: User endpoint reached via StripPrefix
