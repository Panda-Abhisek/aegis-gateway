info:
  name: Delete User
  type: http
  seq: 6
  tags:
    - User Service

http:
  method: DELETE
  url: "{{baseUrl}}/users/:id"
  params:
    - name: id
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: Deleted
    request:
      url: "{{baseUrl}}/users/:id"
      method: DELETE
      params:
        - name: id
          value: ""
          type: path
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: application/json
      body:
        type: json
        data: |-
          {
            "id": "",
            "message": ""
          }
