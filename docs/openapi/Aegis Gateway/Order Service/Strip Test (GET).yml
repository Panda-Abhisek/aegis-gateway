info:
  name: Strip Test (GET)
  type: http
  seq: 7
  tags:
    - Order Service

http:
  method: GET
  url: "{{baseUrl}}/orders/strip-test"
  headers:
    - name: Authorization
      value: Bearer {{JWT}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/orders/strip-test"
      method: GET
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: text/plain
      body:
        type: text
        data: Order endpoint reached via StripPrefix
