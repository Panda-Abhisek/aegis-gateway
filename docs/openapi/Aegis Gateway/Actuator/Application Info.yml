info:
  name: Application Info
  type: http
  seq: 2
  tags:
    - Actuator

http:
  method: GET
  url: "{{baseUrl}}/actuator/info"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/actuator/info"
      method: GET
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: application/json
      body:
        type: json
        data: |-
          {
            "app": {
              "name": "",
              "version": ""
            }
          }
